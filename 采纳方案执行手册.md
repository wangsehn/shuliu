# 采纳方案执行手册（AI 可执行版）

> **项目**：书流（shuliu）推荐系统补全 + 作品集包装
> **仓库**：https://github.com/wangsehn/shuliu（私有，批次 C 转 Public）
> **工作分支**：`recommendation-system`
> **代码根目录**：`c:\Users\57271\Desktop\find work\test\first_app`（即仓库根）
> **文档基线**：内容包 content.js?v=18（10 本 / 76 条 / 26 章）；引擎 recommendation.js?v=3（RECO_VERSION=v2-decay-1）
> **执行原则**：每一步完成后必须执行该步的「验证」命令并核对「预期结果」，不通过则按「异常处理」排查，禁止跳步。

---

## 全局约定（执行前必读）

1. **Shell 环境**：Windows PowerShell。**不支持 `&&` / `||` 链接语句，一律用 `;`**。
2. **文件回退风险**：本机存在外部进程间歇性回退 `index.html` / `app.css` 的现象（疑似编辑器未保存缓冲覆盖）。**每个文件编辑完成后必须立即复读该文件的目标行确认改动仍在**；若发现回退，重新执行该次编辑。
3. **版本号策略**：所有静态资源引用带 `?v=N` 参数，本次执行统一升到 **v=37**。SW 缓存名从 `shuliu-runtime-v1` 升到 `shuliu-runtime-v2`（强制 PWA 用户刷新预缓存）。
4. **测试三件套命令**（均在代码根目录执行，需本地 HTTP 服务，见 0.2）：
   - Node 层：`node tests/data_check.js`、`node tests/replay_eval.js`、`node tests/perf_bench.js`
   - 浏览器层：`node tests\_browser_check.tmp.js`（临时脚本，全文见附录 A；unit 入口**必须带 `?test=1`**，否则测试钩子不暴露）
5. **提交规范**：每个批次一个提交；提交信息中文，格式 `<批次>: <摘要>` + 要点列表。
6. **禁止事项**：不改推荐引擎核心打分逻辑（recommendation.js 仅允许零改动）；不新增功能；不动 PRD V1.1 范围外的事件。

---

## 批次 0：环境自检

### 0.1 git 状态确认

**操作**：
```powershell
git -C "c:\Users\57271\Desktop\find work\test\first_app" status
git -C "c:\Users\57271\Desktop\find work\test\first_app" branch --show-current
git -C "c:\Users\57271\Desktop\find work\test\first_app" log --oneline -3
```

**预期结果**：当前分支为 `recommendation-system`；工作区存在未提交改动（UI 重构相关：app.js / app.css / index.html / www\ 下同名文件），`git log` 顶部为 `e6f19f1`（推荐系统 v2 提交）。

**异常处理**：
- 若分支不是 `recommendation-system`：执行 `git checkout recommendation-system` 后重查。
- 若工作区干净（UI 改动已被回退）：**停止执行**，报告用户「本地 UI 改动丢失」，等待人工确认后从 git 恢复或重做。

### 0.2 本地 HTTP 服务确认

**操作**：
```powershell
try { (Invoke-WebRequest -Uri "http://localhost:8931/index.html" -UseBasicParsing -TimeoutSec 3).StatusCode } catch { "DOWN" }
```

**预期结果**：输出 `200`。

**异常处理**：输出 `DOWN` 时，在代码根目录启动后台服务（用后台任务方式，不要阻塞）：
```powershell
npx http-server "c:\Users\57271\Desktop\find work\test\first_app" -p 8931 -s
```
等 3 秒后重新执行探测命令。

### 0.3 基线回归（改动前先确认全绿）

**操作**：依次执行：
```powershell
node "c:\Users\57271\Desktop\find work\test\first_app\tests\data_check.js"
node "c:\Users\57271\Desktop\find work\test\first_app\tests\replay_eval.js"
node "c:\Users\57271\Desktop\find work\test\first_app\tests\perf_bench.js"
```
再按附录 A 创建 `tests\_browser_check.tmp.js` 并执行 `node tests\_browser_check.tmp.js`。

**预期结果**：data_check 输出 `DATA_CHECK_ALL_PASS`；replay_eval 六项门限全过；perf_bench p95 < 5ms；浏览器层输出 `BROWSER_TESTS_ALL_PASS`（unit 16/16、e2e 14/14）。

**异常处理**：任一基线失败 → 停止，先修复基线再进入批次 A（禁止在红基线上叠改动）。

---

## 批次 A：P0 代码修复

### A1 推荐理由上屏（修复「引擎生成、UI 不展示」断点）

**背景**：`recommendation.js` 的 `buildFeed()` 已为每条结果生成 `recommendationReason` 字段（recommendation.js L247），但 `app.js` 的 `buildFullOrder()` / `rebuildTail()` 只取 `passageId`，理由被丢弃。PRD 验收标准要求「每条推荐都能显示一个真实、可解释的推荐理由」。

**操作 1**：编辑 `app.js`，定位 `buildFullOrder()`（约 L124-128），将：
```javascript
function buildFullOrder() {
  var vis = visiblePassages();
  var user = { interests: S.interests, topicWeights: S.topicWeights, liked: Object.keys(S.likes), saved: Object.keys(S.saves), hidden: S.hidden, seen: Object.keys(S.history) };
  return RECOMMENDER.buildFeed(vis, user, { limit: vis.length }).map(function (p) { return 'p:' + p.passageId; });
}
```
替换为：
```javascript
var _lastReasonMap = {}; /* buildFullOrder/rebuildTail 产出的「片段ID→推荐理由」映射，供 ensureFeed 写入 S.feed */
function buildFullOrder() {
  var vis = visiblePassages();
  var user = { interests: S.interests, interestsAt: S.interestsAt, topicWeights: S.topicWeights, topicLastTouched: S.topicLastTouched, liked: Object.keys(S.likes), saved: Object.keys(S.saves), hidden: S.hidden, seen: Object.keys(S.history) };
  var items = RECOMMENDER.buildFeed(vis, user, { limit: vis.length });
  _lastReasonMap = {};
  items.forEach(function (p) { _lastReasonMap[p.passageId] = p.recommendationReason || ''; });
  return items.map(function (p) { return 'p:' + p.passageId; });
}
```
（注意：此替换同时完成了 A2 在 L126 处的参数补全，A2 只需处理 rebuildTail 一处。）

**操作 2**：编辑 `ensureFeed()`（约 L129-134），将：
```javascript
    S.feed = { roundId: Date.now(), order: buildFullOrder(), cursor: 0 };
```
替换为：
```javascript
    S.feed = { roundId: Date.now(), order: buildFullOrder(), cursor: 0, reasonMap: _lastReasonMap };
```

**操作 3**：编辑 `rebuildTail()`（约 L135-151）。该函数内 user 对象（约 L145）需补衰减参数，且 tail 重建后需同步刷新 reasonMap（已浏览前缀保留原理由）。将：
```javascript
  var user = { interests: S.interests, topicWeights: S.topicWeights, liked: Object.keys(S.likes), saved: Object.keys(S.saves), hidden: S.hidden, seen: viewed.map(function (id) { return id.slice(2); }) };
  var tail = RECOMMENDER.buildFeed(vis, user, { limit: vis.length }).map(function (p) { return 'p:' + p.passageId; });
  tail = keep.concat(tail);
  if (opts && opts.insertFirst) { tail = opts.insertFirst.concat(tail); }
  S.feed.order = viewed.concat(tail);
  persist('feed');
```
替换为：
```javascript
  var user = { interests: S.interests, interestsAt: S.interestsAt, topicWeights: S.topicWeights, topicLastTouched: S.topicLastTouched, liked: Object.keys(S.likes), saved: Object.keys(S.saves), hidden: S.hidden, seen: viewed.map(function (id) { return id.slice(2); }) };
  var items = RECOMMENDER.buildFeed(vis, user, { limit: vis.length });
  var reasonMap = {};
  viewed.forEach(function (id) {
    if (id.charAt(0) === 'p' && S.feed.reasonMap && S.feed.reasonMap[id.slice(2)]) reasonMap[id.slice(2)] = S.feed.reasonMap[id.slice(2)];
  });
  items.forEach(function (p) { reasonMap[p.passageId] = p.recommendationReason || ''; });
  var tail = items.map(function (p) { return 'p:' + p.passageId; });
  tail = keep.concat(tail);
  if (opts && opts.insertFirst) { tail = opts.insertFirst.concat(tail); }
  S.feed.order = viewed.concat(tail);
  S.feed.reasonMap = reasonMap;
  persist('feed');
```

**操作 4**：编辑 `cardHTML()` 的 passage 分支（约 L437-443），在 kicker 之后插入理由行。将：
```javascript
  return '<div class="cardwrap"><div class="card" data-pid="' + p.passageId + '">' +
    '<div class="kicker">' + esc(p.intro) + '</div>' +
```
替换为：
```javascript
  var reason = (S.feed && S.feed.reasonMap && S.feed.reasonMap[p.passageId]) || '';
  return '<div class="cardwrap"><div class="card" data-pid="' + p.passageId + '">' +
    '<div class="kicker">' + esc(p.intro) + '</div>' +
    (reason ? '<div class="reason">' + esc(reason) + '</div>' : '') +
```
（publish 分支不加理由——本机发布不是推荐结果。）

**操作 5**：编辑 `app.css`，在 `.card .kicker::before` 规则之后追加：
```css
.card .reason{flex:0 0 auto;font-size:11px;color:var(--sub);margin-top:5px}
```

**预期结果**：冷启动或带画像的每张片段卡在 Hook 下方显示一行 11px 浅色理由，文案命中引擎 `reasonFor` 四种模板之一：`和你喜欢过的内容相似` / `因为你喜欢过「XX」相关内容` / `因为你选择了「XX」` / `给你一段新的文学发现`。

**验证**：`node tests\_browser_check.tmp.js` 全绿；`S.feed.reasonMap` 已随 feed 持久化（e2e 的 REC-03 断言覆盖，见 A6 操作 2）。

**异常处理**：
- 理由行不显示：检查 `S.feed.reasonMap` 是否存在（`?test=1` 下 `T.setRawState` 构造的 feed 可能缺该字段，cardHTML 已做 `|| ''` 兜底，此时不显示属正常，但 e2e 走真实 onboarding 路径必有值）。
- localStorage 超限：reasonMap 约 76 条 × 30 字符，增量 <5KB，无需处理；若 persist 失败 toast 出现，检查 1MB 上限逻辑是否被其他键挤占。

### A2 衰减参数接通（修复「半衰期在产品路径失效」）

**背景**：引擎 `score()` 通过 `topicLastTouched` 与 `interestsAt` 计算时间衰减；`recordRecommendationFeedback()`（app.js L77-82）已正确传递，但 `buildFullOrder` / `rebuildTail` 的 user 缺这两个字段，导致学到的权重永不衰减。**此问题已在 A1 操作 1、3 中一并修复**（user 对象已补 `interestsAt` / `topicLastTouched`）。

**操作**：无独立代码改动。仅做验证：

**验证**：在 `?test=1` 环境（或临时页）执行断言脚本：
```javascript
/* 伪代码：构造 topicWeights={亲密关系:3}、topicLastTouched={亲密关系:now-30天}、interestsAt=now-30天，
   调 T.buildFullOrder() 前后对比首条是否为高权重但已衰减主题的让位 */
```
正式验证以 A6 新增的 e2e `REC-06`（衰减参数透传）为准。

**预期结果**：30 天前的 `topicWeights=3` 与当天的 `topicWeights=3` 产生的排序不同（衰减因子 0.5^(30/7)≈0.05，前者应几乎不再优先）。

**异常处理**：若 e2e REC-06 失败，检查 `recommendation.js` L53 `interestsAt: user.interestsAt || 0` 与 L80 `user.topicLastTouched[topic]`——确认传入字段名拼写一致（`topicLastTouched` 不是 `topicLastTouch`）。

### A3 取消喜欢/收藏反向修正画像

**背景**：引擎 deltas 已注册 `unlike: -1.0 / unsave: -1.2`（recommendation.js L41），单测已验证可抵消；但 app.js 的 `toggleLike/toggleSave` 只在正向分支发事件。

**操作 1**：编辑 `toggleLike`（约 L512-520），将：
```javascript
  if (persist('likes')) {
    if (!was) recordRecommendationFeedback('like', PASSAGE[pid]);
    btn.classList.toggle('on', !was);
```
替换为：
```javascript
  if (persist('likes')) {
    recordRecommendationFeedback(was ? 'unlike' : 'like', PASSAGE[pid]);
    btn.classList.toggle('on', !was);
```

**操作 2**：编辑 `toggleSave`（约 L521-529），将：
```javascript
  if (persist('saves')) {
    if (!was) recordRecommendationFeedback('save', PASSAGE[pid]);
    btn.classList.toggle('on', !was);
```
替换为：
```javascript
  if (persist('saves')) {
    recordRecommendationFeedback(was ? 'unsave' : 'save', PASSAGE[pid]);
    btn.classList.toggle('on', !was);
```

**预期结果**：喜欢 → 权重 +1.0 → 取消喜欢 → 权重回到 0（±浮点误差）；事件流出现成对 `like`/`unlike` 记录。收藏同理（+1.2 / −1.2）。

**异常处理**：`applyFeedback` 对 topics 为空的事件直接忽略（L271），内容包片段均有主题，不会触发；若权重未回落，检查是否误传 `S.likes` 键名（应传 `PASSAGE[pid]` 整个片段对象）。

### A4 双击点赞统一埋点

**背景**：`mountFeed()` 的双击 handler（约 L401-408）只写 `S.likes`，未走推荐事件入口，与点 ❤ 按钮口径不一致（同一意图、埋点不同）。

**操作**：编辑 `mountFeed()` 双击分支，将：
```javascript
    $('.card', feed).addEventListener('dblclick', function () {
      var pid = item.p.passageId;
      if (!S.likes[pid]) {
        S.likes[pid] = Date.now();
        if (persist('likes')) { toast('已喜欢'); var b = $('[data-act="like"]', feed); if (b) b.classList.add('on'); }
      } else { toast('已经在喜欢的列表里了'); }
    });
```
替换为：
```javascript
    $('.card', feed).addEventListener('dblclick', function () {
      var pid = item.p.passageId;
      if (!S.likes[pid]) {
        S.likes[pid] = Date.now();
        if (persist('likes')) {
          recordRecommendationFeedback('like', PASSAGE[pid]); /* 与点 ❤ 按钮同一事件入口，口径统一 */
          toast('已喜欢'); var b = $('[data-act="like"]', feed); if (b) b.classList.add('on');
        }
      } else { toast('已经在喜欢的列表里了'); }
    });
```

**预期结果**：双击点赞后 `recEvents` 出现该 passageId 的 `like` 事件（与按钮路径完全同构）。

**异常处理**：双击与上滑手势冲突（touchend 在 dblclick 前触发 quick_skip）——现有行为即如此（点 ❤ 也会先经历曝光），不属本次修复范围，不在 V1 处理。

### A5 版本号与 SW 缓存升级

**操作 1**：编辑 `index.html` L14 与 L23：
- `<link rel="stylesheet" href="app.css?v=36">` → `app.css?v=37`
- `<script src="app.js?v=35"></script>` → `app.js?v=37`

**操作 2**：编辑 `sw.js` L4：`var CACHE = 'shuliu-runtime-v1';` → `var CACHE = 'shuliu-runtime-v2';`

**预期结果**：三个文件中版本参数全部为 v=37（index.html、www\index.html 同步后），SW 缓存名 v2。

**异常处理**：编辑后复读确认（全局约定第 2 条）；若 index.html 再次回退，重做该编辑并在批次 A 提交前最后复查一次 `git diff index.html`。

### A6 全量回归 + 新增 e2e 用例

**操作 1**：编辑 `tests/e2e.html`，在测试数组（REC-02 之后）追加 3 条用例：
```javascript
['REC-03 推荐理由上屏', async function () {
  var r = doc().querySelector('#feed .card .reason');
  if (!r) throw new Error('理由行未渲染');
  if (!/因为|给你|相似/.test(r.textContent)) throw new Error('理由文案不匹配引擎模板: ' + r.textContent);
  return r.textContent.slice(0, 24);
}],
['REC-04 取消喜欢回退画像权重', async function () {
  var b = doc().querySelector('[data-act="like"]');
  b.click(); await sleep(120);
  var pid = doc().querySelector('.card').dataset.pid;
  var p = win().CONTENT.passages.filter(function (x) { return x.passageId === pid; })[0];
  var tw1 = (ls('shuliu.v1.topicWeights') || {})[p.topics[0]] || 0;
  b.click(); await sleep(120);
  var tw2 = (ls('shuliu.v1.topicWeights') || {})[p.topics[0]] || 0;
  if (!(tw1 >= 0.9)) throw new Error('点赞后权重未+1: ' + tw1);
  if (Math.abs(tw2 - (tw1 - 1)) > 0.01) throw new Error('取消喜欢未回退: ' + tw1 + '->' + tw2);
  var evs = ls('shuliu.v1.recEvents') || [];
  if (!evs.some(function (e) { return e.type === 'unlike' && e.passageId === pid; })) throw new Error('unlike 事件缺失');
  return tw1 + ' -> ' + tw2;
}],
['REC-05 双击点赞产生 like 埋点', async function () {
  var card = doc().querySelector('.card');
  var pid = card.dataset.pid;
  card.dispatchEvent(new MouseEvent('dblclick', { bubbles: true }));
  await sleep(150);
  var evs = ls('shuliu.v1.recEvents') || [];
  if (!evs.some(function (e) { return e.type === 'like' && e.passageId === pid; })) throw new Error('双击未产生 like 事件');
  return 'ok';
}],
['REC-06 衰减参数透传（30 天前权重让位当日权重）', async function () {
  /* 直接调引擎：同权重不同时间戳，排序应不同 */
  var R = win().SHULIU_RECOMMENDER;
  var now = Date.now(), day = 86400000;
  var pool = win().CONTENT.passages.filter(function (p) { return p.topics.indexOf('亲密关系') >= 0; }).slice(0, 3);
  var mk = function (ts) {
    return { interests: [], interestsAt: ts, topicWeights: { '亲密关系': 2 }, topicLastTouched: { '亲密关系': ts }, liked: [], saved: [], hidden: [], seen: [], events: [] };
  };
  var fresh = R.buildFeed(pool, mk(now - day), {});
  var stale = R.buildFeed(pool, mk(now - 30 * day), {});
  var scoreOf = function (arr) { return arr[0].passageId === pool[0].passageId ? 1 : 0; };
  if (scoreOf(fresh) === scoreOf(stale)) throw new Error('衰减未生效：新旧时间戳排序一致');
  return 'fresh=' + fresh[0].passageId + ' stale=' + stale[0].passageId;
}],
```
> 注意：`sleep` / `doc()` / `win()` / `ls()` 用 e2e.html 中已有的同名工具函数；若函数名不同（如 `page.evaluate` 封装名不同），以文件内现有 REC-01/REC-02 的写法为准对齐，**不得引入新全局**。REC-04 前置条件：当前卡片主题权重初始为 0（e2e 每次全量清 localStorage 后 onboarding，满足）。

**操作 2**：执行全量回归：
```powershell
node "c:\Users\57271\Desktop\find work\test\first_app\tests\data_check.js"
node "c:\Users\57271\Desktop\find work\test\first_app\tests\replay_eval.js"
node "c:\Users\57271\Desktop\find work\test\first_app\tests\perf_bench.js"
node "c:\Users\57271\Desktop\find work\test\first_app\tests\_browser_check.tmp.js"
```

**预期结果**：data_check `DATA_CHECK_ALL_PASS`；replay_eval 门限全过（理由字段不改变排序，重放指标应与基线一致）；perf p95 < 5ms；浏览器层 **unit 16/16、e2e 18/18**、UI 结构断言 7/7，输出 `BROWSER_TESTS_ALL_PASS`。

**异常处理**：
- REC-03 失败多为 `?test=1` 路径下 feed 由 `T.setRawState` 构造（无 reasonMap）→ 修正 e2e 前置：走真实 onboarding 流程（清 localStorage → reload → 跳过引导页）后再断言。
- REC-04 失败若为 persist 还原（`loadState()` 回滚），说明 STORE 写入失败，检查 1MB 上限。
- e2e 计数变化后，附录 A runner 的 `e2e.total` 断言无需改（runner 只看 failed 数）。

### A7 同步安卓实测

**操作**：
```powershell
Copy-Item "app.js","app.css","index.html","sw.js" "www\" -Force
npx cap sync android
.\android\gradlew.bat -p android assembleDebug --console=plain
adb install -r "android\app\build\outputs\apk\debug\app-debug.apk"
adb shell am force-stop com.shuliu.reader
adb shell am start -n com.shuliu.reader/.MainActivity
Start-Sleep -Seconds 6
adb shell screencap -p /sdcard/a7.png
adb pull /sdcard/a7.png "$env:TEMP\batchA.png"
adb shell rm /sdcard/a7.png
```

**预期结果**：安装 Success；截图可见：理由行显示在 Hook 下方；喜欢一张卡后理由变为「和你喜欢过的内容相似」或「因为你喜欢过…」。

**异常处理**：
- gradlew 报签名/版本冲突：先 `adb uninstall com.shuliu.reader` 再 install。
- 截图看不到理由行：确认 www\index.html 的 `?v=37`（`Select-String -Path "www\index.html" -Pattern "v=37"`），cap sync 输出应含 `Sync finished`。

### A8 批次 A 提交推送

**操作**：
```powershell
git add app.js app.css index.html sw.js tests/e2e.html www/
git commit -m @'
批次A: 推荐系统产品链路补全（P0 修复）

- 推荐理由上屏：buildFeed 的 recommendationReason 经 reasonMap 持久化并在卡片渲染（PRD AC：每条推荐可解释）
- 时间衰减接通：buildFullOrder/rebuildTail 补传 topicLastTouched/interestsAt，7天半衰期在真实 Feed 生效
- unlike/unsave 反向修正：取消喜欢/收藏回退画像权重（-1.0/-1.2），事件成对落账
- 双击点赞统一埋点：与喜欢按钮同一事件入口，消除口径不一致
- 新增 e2e：REC-03~06；版本 v37；SW 缓存 v2
'@
git push origin recommendation-system
```

**预期结果**：push 成功，GitHub 分支可见新提交。

**异常处理**：PowerShell here-string 中的中文/换行异常时，改用 `git commit -m "批次A: 推荐系统产品链路补全（理由上屏/衰减接通/反向修正/统一埋点）"` 单行提交。

---

## 批次 B：文档一致性

### B1 推荐系统 PRD 状态与事件分级

**操作**：编辑 `推荐系统核心功能PRD.md`：
- L5 `状态：待开发` → `状态：V1 已上线（recommendation-system 分支）；V1.1 规划中`
- 文档头部状态行之后插入「实现状态对照表」：

```markdown
## 实现状态对照（V1 已上线 / V1.1 规划）

| 事件 | 状态 | 说明 |
|---|---|---|
| like / unlike | ✅ V1 已上线 | 含按钮与双击两条路径，口径统一 |
| save / unsave | ✅ V1 已上线 | 取消收藏反向修正画像 |
| view_complete / quick_skip | ✅ V1 已上线 | 停留时长 ≥4s 判定，连续 2 次划走才计负分 |
| open_original | ✅ V1 已上线 | 「查看原文 →」入口 |
| not_interested | ✅ V1 已上线 | 即时 -0.5 并隐藏片段 |
| impression | ⏳ V1.1 | 曝光事件（当前以 S._cardShownAt 内存态代替，未落账） |
| continue_reading | ⏳ V1.1 | 阅读页续读埋点（续读功能本身已上线） |
| share | ⏳ V1.1 | 分享动作埋点 |

> V1 上线口径：6 个核心行为事件闭环 + 推荐理由上屏 + 时间衰减全链路生效（批次 A 交付）。
```

**预期结果**：PRD 不再出现「待开发」；面试官按 PRD 追问时每个 ✅ 都可运行验证。

**异常处理**：若 PRD 中另有「里程碑/排期」章节与新状态冲突，在其后加一行「（排期为规划稿，实际交付以对照表为准）」，不删除原文。

### B2 全仓陈旧口径巡检

**操作**：
```powershell
Select-String -Path "*.md","..\prd\*.md" -Pattern "app\.js\?v=2\d|app v2\d|2 本|20 条|待开发" | Select-Object Filename, LineNumber, Line
```
对每个命中：确认为历史记录语境（如「当时是 2 本」）则加「（历史口径）」前缀；为现状描述则更新为「10 本 / 76 条 / content v18 / app v37 / reco v3」。已知重点：`调试报告.md`（停留在 v28/2本/20条）。

**预期结果**：上述 grep 对「现状描述类」命中为 0（历史记录类允许带标注保留）。

**异常处理**：`..\prd` 路径不存在时用绝对路径 `C:\Users\57271\Desktop\find work\prd\*.md` 重试。

### B3 验收记录追加批次 A 章节

**操作**：在 `验收记录.md` 末尾追加：
```markdown
## 推荐系统链路补全（批次 A，v37）

- 修复：推荐理由上屏 / 衰减参数透传 / unlike+unsave 反向修正 / 双击点赞统一埋点
- 测试：unit 16/16，e2e 18/18（新增 REC-03 理由上屏、REC-04 unlike 回退、REC-05 双击埋点、REC-06 衰减透传），data_check / replay_eval / perf_bench 全过
- 口径：10 本 / 76 条 / content v18 / app v37 / reco v3 / SW runtime-v2
```

**预期结果**：记录与代码一致，后续面试可引用。

### B4 批次 B 提交

**操作**：`git add 推荐系统核心功能PRD.md 调试报告.md 验收记录.md`（及 B2 实际改动的文件）→ 提交信息 `批次B: 文档口径对齐 v37（PRD 状态对照表 / 陈旧数据巡检 / 验收记录）` → push。

---

## 批次 C：作品集包装

### C1 隐私巡检（转 Public 前置，必须人工确认）

**操作**：
```powershell
git grep -n -E "1[3-9][0-9]{9}" ; git grep -n -iE "api[_-]?key|secret|password|token\s*[:=]"
git grep -n -E "[a-zA-Z0-9._%+-]+@(qq|163|gmail|outlook)\.[a-z]+"
```
同时人工检查：`打包说明.md`（可能含签名密码/个人路径）、`验收截图\`（可能含个人数据）、`android\` 下 keystore 文件是否被 git 跟踪（`git ls-files | Select-String keystore`）。

**预期结果**：无手机号 / 邮箱 / 密钥 / keystore 被跟踪。

**异常处理**：发现敏感项 → 停止批次 C，报告用户决定（移除或放弃转 Public）。**此步结果必须展示给用户确认后才能执行 C3。**

### C2 README（面向招聘者的 Case Study 首页）

**操作**：在仓库根创建 `README.md`，结构如下（正文数据取自 `tests/replay_eval.js` 输出与 `验收记录.md`，禁止编造数字）：

```markdown
# 书流 Shuliu — 用「刷」的方式打开经典文学
> 片段流 → 兴趣冷启动 → 行为学习 → 个性化推荐 → 进入原文

（一段 50 字以内的定位陈述）

## 01 问题
经典文学阅读启动成本高：用户不知道从哪读起。
## 02 核心机制
（信息流机制 + 文学内容；配架构流程图：兴趣选择→行为→画像→衰减排序→多样性约束→下一轮）
## 03 我做了什么
产品定位 / UX / PRD / 推荐策略 / 埋点体系 / 原型开发 / 测试评估（一句话各带证据链接）
## 04 推荐系统设计
（6 事件闭环表 + 约束规则 + 推荐解释；链接 PRD）
## 05 Baseline vs V2（离线重放评估）
（4 合成人设 × 24 轮；表格呈现命中率对比，含 P3 人设后程 +48.7pp；说明"未宣称更好，而是用重放验证学习能力"）
## 06 产品演示
（GIF / 截图：选兴趣 → 刷片段 → 喜欢 → 理由变化 → 查看原文）
## 07 下一步
真实用户测试 → 云端 → A/B → 混合推荐
```

**预期结果**：3 分钟可读完；所有数字可溯源到验收记录。

**异常处理**：replay_eval 数字与记忆不符时以当前 `node tests/replay_eval.js` 实际输出为准。

### C3 转 Public

**操作**（C1 确认通过后）：
```powershell
gh repo edit wangsehn/shuliu --visibility public --accept-visibility-change-consequences
```

**预期结果**：`gh repo view wangsehn/shuliu --json visibility` 返回 `PUBLIC`。

**异常处理**：gh 版本不识别 `--accept-visibility-change-consequences` 时去掉该参数重试；仍失败则提示用户网页端操作（Settings → Danger Zone → Change visibility）。

### C4 GitHub Pages 上线

**操作**：
```powershell
gh api repos/wangsehn/shuliu/pages -X POST -f "source[branch]=recommendation-system" -f "source[path]=/"
```
验证：`https://wangsehn.github.io/shuliu/` 可打开且能完成 onboarding（用浏览器自动化打开一次，确认 PWA 可用、无控制台错误）。

**预期结果**：Pages 返回 201；线上可玩。

**异常处理**：Pages 首次构建需等待（轮询 `gh api repos/wangsehn/shuliu/pages`，`status: building` 时每 30s 查一次，最多 10 次）；若 SW 在 Pages 域名下报路径错误，检查 `sw.js` 与 `manifest` 均使用相对路径（当前已是 `./`，预期无需改）。

### C5 演示 GIF（30-60 秒）

**操作**：
```powershell
adb shell screenrecord --time-limit 45 /sdcard/demo.mp4
#（录屏期间人工/自动化执行：冷启动选兴趣 → 上滑 2 次 → 喜欢 1 张 → 观察理由变化 → 点「查看原文 →」）
adb pull /sdcard/demo.mp4 "$env:TEMP\demo.mp4"
ffmpeg -i "$env:TEMP\demo.mp4" -vf "fps=12,scale=360:-1" -loop 0 docs\demo.gif
```
README 的 06 节引用 `docs/demo.gif`。

**预期结果**：GIF < 8MB，README 正常显示。

**异常处理**：无 ffmpeg → 保留 mp4 并 `git add docs/demo.mp4`，README 改引 mp4；screenrecord 需真机/模拟器实机操作，无法纯后台自动化时请用户配合录制 45 秒。

### C6 批次 C 提交推送

**操作**：`git add README.md docs/` → 提交 `批次C: 作品集包装（Case Study 首页 / Pages / 演示 GIF）` → push。

---

## 附录 A：浏览器回归临时脚本（tests\_browser_check.tmp.js）

每次批次 A 回归前重建此文件（跑完删除，不入库）。要点：
- puppeteer-core 驱动 `C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe`，headless
- unit 入口 **必须** `tests/unit.html?test=1&run=<timestamp>`，等待 `window.UNIT_RESULT`
- e2e 入口 `tests/e2e.html?run=<timestamp>`，先 `localStorage.clear()` + reload，等待 `window.E2E_RESULT`
- UI 断言：`.card` 背景透明/圆角 0；`.kicker::before` 含 ✦；`.card .text` 18px；kicker→正文间距 24-40px；`.src .go` 文案为 `查看原文 →`；`.actions button` 无文字标签 span；`.actions` 为 `absolute/right:0px`
- 判定输出：unit/e2e failed=0 且断言全过 → `BROWSER_TESTS_ALL_PASS`

## 附录 B：验收总表

| 批次 | 项 | 通过标准 |
|---|---|---|
| 0 | 基线回归 | data_check / replay / perf / 浏览器 全绿 |
| A1 | 理由上屏 | e2e REC-03 过；模拟器截图可见理由行 |
| A2 | 衰减透传 | e2e REC-06 过（新旧时间戳排序不同） |
| A3 | 反向修正 | e2e REC-04 过（+1.0 → 回 0，unlike 事件在） |
| A4 | 统一埋点 | e2e REC-05 过（双击产生 like 事件） |
| A5 | 版本 | 全部资源 ?v=37；SW runtime-v2 |
| A6 | 回归 | unit 16/16，e2e 18/18，perf p95<5ms，replay 门限过 |
| A7 | 安卓 | 新 APK 实测截图通过 |
| B | 文档 | 「待开发」清零；现状口径统一 v37；验收记录含批次 A |
| C | 包装 | 隐私巡检通过 + 用户确认；README 3 分钟可读；Pages 可玩；GIF <8MB |
