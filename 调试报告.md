# 书流 first_app · 最终调试报告

## 0. 检查信息

| 项 | 内容 |
|---|---|
| 日期 | 2026-09-07 |
| 被测版本 | `test/first_app`（web 源码 + `www/` + Capacitor APK assets，三副本哈希一致） |
| 方法 | 静态检查（语法/引用/哈希比对）+ headless Edge 自动化测试 + Node 数据校验 + Puppeteer/CDP SW 实测 + 模拟器冒烟 |
| 环境 | Windows 11 · Edge headless (new) · my_phone AVD（Android 16 / API 36）· 本地静态服务 :8931 |
| 依据 | 《书流 PRD V1.0》FR/PG/AC 全表、《验收记录.md》AC-01~24 |

**总体结论：全部检查项通过。本轮新发现并修复 2 个问题（均属低概率但真实的边界缺陷），修复后全量回归绿灯，可进入最终验收。**

## 1. 已验证通过的项目模块

### 1.1 静态完整性与构建一致性
| 检查项 | 结果 | 证据 |
|---|---|---|
| JS 语法（app/store/content/sw） | PASS | `node --check` 四文件零错误 |
| 三副本一致性（根目录 / `www/` / APK `assets/public/`） | PASS | 6 个核心文件 SHA-256 逐一比对相同 |
| index.html 脚本引用 | PASS | content.js?v=17 / store.js?v=17 / app.js?v=28 均存在 |
| PWA manifest | PASS | name/short_name/start_url/scope/display/icons 全齐 |
| 图标资源 | PASS | manifest 引用的 4 个图标 + apple-touch-icon + icon-1024 共 6 个全部存在 |

### 1.2 单元测试（tests/unit.html，?test=1）
**13/13 通过，0 失败。** 覆盖：
- code point 工具（cpLen/sliceCP 含增补平面字符、esc 转义）
- mixPool 双池混排规范（6M/2O、单池、1M/10O 与 3M/1O 边界）
- shuffle 排列不变性（同元素同长度、不改入参）
- buildFullOrder（p: 前缀、排除 hidden、与规范逐位一致）
- chTitle 正常/缺章降级
- 全部 20 条片段坐标 verifyLoc 重建
- store.js 持久化往返 + 损坏 JSON 回退

### 1.3 集成测试（tests/e2e.html，iframe 真实 DOM 交互）
**12/12 通过，0 失败。** 覆盖 AC-01/02a/02b/02c、AC-04 上滑切卡、AC-06 混排逐位断言、AC-07 喜欢持久化（重载验证）、AC-09 原文定位高亮、AC-10 返回保位、AC-22 不感兴趣+恢复、AC-23 损坏数据可恢复、AC-24 清除记录。

### 1.4 响应式布局（5 视口 × 3 页面态，headless 实测 DOM 断言）
| 视口 | 起始页 | 首页 | 阅读页 | 横向溢出 |
|---|---|---|---|---|
| 320×700（小屏） | ✓ | ✓（feed+3 tab） | ✓（mark 高亮） | 无 |
| 360×740（基准） | ✓ | ✓ | ✓ | 无 |
| 430×932（大屏） | ✓ | ✓ | ✓ | 无 |
| 768×1024（平板） | ✓ | ✓ | ✓ | 无 |
| 1280×800（桌面） | ✓ | ✓ | ✓ | 无 |

每态断言：`scrollWidth ≤ clientWidth`（documentElement 与 body 双口径）、导航 3 tab、卡片渲染、阅读页 `<mark>` 存在。

### 1.5 数据完整性（Node 独立校验，镜像 app.js verifyLoc 口径）
| 检查项 | 结果 |
|---|---|
| 结构 | 2 书（红楼梦 4 章 / 西游记 3 章，共 7 章 4.81 万字）、20 条片段 |
| 坐标口径 | 20/20 条 `loc{sp,so,ep,eo}` 按段落索引+码位偏移重建与 `text` 逐字一致；`cpLength` 与实际码位数一致 |
| 字段 | 片段 id 无重复；bookId/chapterId 引用有效且相互一致；intro 全有 |
| 文本 | 片段字数 84~144（符合 40~320 规范）；章节段落完整无截断特征 |
| 主题分布 | 自我认识:4 亲密关系:3 孤独与陪伴:3 成长与选择:4 情绪与压力:3 人性与社会:3 —— 六兴趣均 ≥3 条匹配，任取 3 兴趣必有多余其他池，「3 匹配+1 其他」结构恒成立 |

### 1.6 PWA / Service Worker 离线（Puppeteer + CDP 实测）
| 检查项 | 结果 |
|---|---|
| SW 注册 | PASS：scope `/`，`navigator.serviceWorker.controller = true` |
| 预缓存 | PASS：`shuliu-runtime-v1` 含 `/` 与 `/index.html` |
| **离线重载** | **PASS：断网 reload 后应用正常启动（起始页渲染），全程 0 控制台错误** |

### 1.7 Android APK（Capacitor 6，my_phone AVD / API 36）
- 签名 release APK 安装、冷启动正常；首页渲染完整（含「继续阅读」历史条）——[emu_final_smoke.png](验收截图/emu_final_smoke.png)
- 返回键四场景已在上轮修复后回归：弹层关闭 ✓ / 阅读页→首页 ✓ / 我的页→首页 ✓ / 首页退出 ✓（APK 内不注册 SW，由原生外壳加载，无缓存旧资源风险）

### 1.8 性能与资源占用（headless 实测）
| 指标 | 数值 | 评价 |
|---|---|---|
| DOM ready（首载） | 20~60 ms | 极快 |
| 首载传输量 | 约 233 KB（4 个资源；二次加载走缓存 0 KB） | 轻量 |
| JS 堆内存 | 1.66~3.03 MB（320px→1280px） | 远低于关注阈值 |
| localStorage 占用 | 约 630 B（全量状态） | 极小 |
| APK 体积 | 约 3.0 MB（base.apk） | 轻量 |

### 1.9 兼容性
| 检查项 | 结果 |
|---|---|
| JS 方言 | 4 个 JS 文件 ES6+ token 扫描为 0（纯 ES5），兼容 Android WebView 40+/iOS 9+ |
| CSS 特性 | 仅用 flex `gap`（WebView 84+，2020 年起）与 `env(safe-area-inset-*)`（带 0px 回退）；未用 dvh/:has/backdrop-filter 等高危特性 |
| PWA 环境守卫 | 修复后：Capacitor 内不注册 SW（见 2.1）；http(s) 正常注册 |
| 数据健壮性 | 损坏 JSON 回退默认值不崩溃（单测覆盖）；缺章降级页（AC-11/23） |

## 2. 本轮发现并修复的问题

### 2.1 index.html SW 注册守卫在 Capacitor 6 下失效（中风险，已修复）
- **现象**：守卫用 `/Capacitor/i.test(navigator.userAgent)` 判断原生环境，但 Capacitor 6 默认不向 UA 追加 "Capacitor" 字样（与上轮返回键问题同根因），导致 APK 内 WebView 会注册 SW。
- **影响**：SW 的 cache-first 资源策略在 APK 升级后（版本参数不变时）可能供应旧脚本，造成「更新了 APK 却跑旧代码」。
- **修复**：index.html 守卫改为 `window.Capacitor.isNativePlatform()` 优先判定（UA 正则保留作双保险）；已同步三副本并重签 APK。
- **回归**：模拟器冒烟启动正常；web 端 SW 离线实测仍通过（守卫只影响原生环境）。

### 2.2 测试页版本参数漂移（低风险，已修复）
- **现象**：tests/unit.html 引用 `app.js?v=27`，主应用为 `?v=28`，存在测试载入旧脚本的可能。
- **修复**：unit.html 对齐至 v=28；本轮单测/e2e 即在修复后重跑全绿。

## 3. 遗留与建议（不阻塞验收）
1. **真机手势实测**：惯性滚动、长按系统菜单、安全区实际 inset 需真机抽查（模拟器与合成事件无法覆盖）。
2. **低端设备抽查**：2× dpr、320px 宽的低端 Android 设备跑一遍核心链路。
3. **iOS 路径**：`apple-touch-icon`/meta 已备，但未做 iOS Safari 实测；如需上架 App Store 需另行规划。
4. **SW 缓存增长**：runtime cache 无上限，当前资源量（<300 KB）无碍；第二批书籍上线时建议按版本参数自然换新即可，无需改造。
5. **测试工具链**：本轮新增 `tests/data_check.js`（Node 数据校验）与 `tests/sw_offline_check.js`（SW 离线回归，依赖 `puppeteer-core`），可纳入后续回归流程。

## 4. 产物清单
| 产物 | 路径 |
|---|---|
| Web 应用 | `test/first_app/`（index.html + app.js?v=28 + app.css + store.js + content/content.js?v=17） |
| Android APK | `android/app/build/outputs/apk/release/app-release.apk`（签名 shuliu-release.keystore，已装模拟器） |
| 打包说明 | `打包说明.md` |
| 验收记录 | `验收记录.md`（AC-01~24 + 10 项历史修复） |
| 本报告 | `调试报告.md` |
| 证据截图 | `验收截图/`（浏览器各态 + 模拟器 emu_* 系列 + unit/e2e 结果快照 + sw_offline_result.json） |
| 测试 | `tests/unit.html`（13 项）、`tests/e2e.html`（12 项）、`tests/data_check.js`、`tests/sw_offline_check.js` |
