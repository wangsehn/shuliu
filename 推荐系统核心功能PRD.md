# 书流推荐系统核心功能 PRD

版本：V1.0

状态：V1 已上线（recommendation-system 分支）；V1.1 规划中

目标平台：书流 Web / Android App

## 实现状态对照（V1 已上线 / V1.1 规划）

| 事件 | 状态 | 说明 |
|---|---|---|
| like / unlike | ✅ V1 已上线 | 含 ❤ 按钮与双击正文两条路径，事件口径统一；取消喜欢回退画像权重 |
| save / unsave | ✅ V1 已上线 | 取消收藏反向修正画像（-1.2） |
| view_complete / quick_skip | ✅ V1 已上线 | 停留时长 ≥4s 判定完读；连续 2 次同主题划走才计负分（防误伤） |
| open_original | ✅ V1 已上线 | 「查看原文 →」入口触发 |
| not_interested | ✅ V1 已上线 | 即时 -0.5 并隐藏该片段 |
| impression | ⏳ V1.1 | 曝光事件（当前以内存态 S._cardShownAt 支撑完读判定，未落账） |
| continue_reading | ⏳ V1.1 | 阅读页续读埋点（续读功能本身已上线） |
| share | ⏳ V1.1 | 分享动作埋点（分享卡功能已上线） |

> V1 上线口径：6 个核心行为事件闭环 + 推荐理由上屏 + 时间衰减全链路生效 + unlike/unsave 反向修正（批次 A 交付，v37）。

## Problem Statement

书流当前使用兴趣选择和“3 个匹配 + 1 个其他”的固定混排规则。这个规则能完成冷启动，但它不会根据用户真实行为持续学习：用户喜欢、收藏、进入原文、快速划走或点击“不感兴趣”之后，后续内容变化有限。

当前内容规模较小，推荐结果还容易重复；用户也不知道为什么会看到某个片段。若直接引入复杂机器学习模型，数据量又不足以支撑可靠训练。

本需求要建立一个可解释、可测量、可逐步升级的推荐系统，让书流能从“按兴趣混排片段”升级为“根据用户阅读反馈选择下一段可能击中他的文字”。

## Solution

建立由五层组成的推荐系统：

1. 内容标签层：为书、章节和片段建立主题、情绪、文风、难度等特征。
2. 行为反馈层：记录曝光、停留、划走、喜欢、收藏、进入原文、继续阅读和不感兴趣等事件。
3. 候选生成层：从兴趣匹配、相似片段、热门内容、编辑精选、新内容和探索池生成候选集。
4. 排序与组装层：根据用户偏好和内容特征计算分数，完成去重、同书限制、多样性和探索比例控制。
5. 评估层：记录推荐结果和后续行为，对比现有 3:1 混排基线，持续调整规则。

第一版在本地完成计算，不依赖云服务器和机器学习模型。用户数据结构稳定后，再将行为和推荐计算迁移到云端，并考虑混合推荐模型。

## Goals

- 新用户在没有历史数据时，能快速得到符合兴趣且有一定探索性的片段。
- 用户明确表达喜欢、收藏或不感兴趣后，后续推荐能在合理范围内发生变化。
- Feed 不连续重复同一本书、同一主题或已经看过的片段。
- 推荐结果保持文学多样性，避免只推容易理解的金句。
- 每条推荐都能解释为“因为你选择了某兴趣”“因为你喜欢过相似内容”或“编辑精选”。
- 能用统一数据记录评估推荐是否带来更高的进入原文率、继续阅读率和收藏率。

## Non-goals

- 第一版不训练深度学习模型。
- 第一版不做实时协同过滤，不依赖大量用户之间的相似行为。
- 第一版不采集精确地理位置、通讯录或设备指纹。
- 第一版不做强制登录；未登录用户也能使用核心阅读体验。
- 第一版不以停留时长作为唯一目标，不追求最大化刷屏时长。
- 第一版不建设复杂的运营后台，只保留可导出的行为数据和人工内容配置能力。

## User Stories

1. 作为新用户，我想选择最多三个阅读兴趣，以便第一次打开书流时看到与我相关的片段。
2. 作为新用户，即使我没有选择兴趣，我也想看到有质量的内容，而不是空白页面。
3. 作为读者，我想看到不同主题和不同书籍的片段，以免推荐很快变得单调。
4. 作为读者，我想喜欢一条片段，以便系统逐渐理解我的偏好。
5. 作为读者，我想收藏一条片段，以便之后回看，并让系统知道这是一条强兴趣信号。
6. 作为读者，我想点击“不感兴趣”，以便减少相似内容。
7. 作为读者，我想看到“不感兴趣”的影响范围，避免一次操作误伤整个主题。
8. 作为读者，我想随时调整兴趣，以便改变推荐方向。
9. 作为读者，我想知道某条片段为什么被推荐，以便理解并纠正系统。
10. 作为读者，我想进入原文，以便从片段延伸到完整章节。
11. 作为读者，我想继续阅读上次进入的章节，以便形成完整阅读而不是只刷摘要。
12. 作为读者，我想看到一些完全不同但可能有启发的片段，以便发现未知兴趣。
13. 作为读者，我不希望已经看过的片段频繁出现，除非它被标记为回顾内容。
14. 作为读者，我不希望同一本书连续占满 Feed，以便保持探索感。
15. 作为内容编辑，我想为片段设置固定标签，以便推荐规则使用一致的内容特征。
16. 作为内容编辑，我想设置片段质量和精选状态，以便控制低质量内容的曝光。
17. 作为内容编辑，我想下架片段，以便错误、重复或不适合展示的内容立即停止推荐。
18. 作为产品负责人，我想查看不同推荐策略的结果，以便决定是否保留新的排序规则。
19. 作为产品负责人，我想区分曝光、打开、读完和进入原文，以便避免用单一点击率误判推荐质量。
20. 作为开发者，我想在没有云端服务时运行推荐系统，以便先在真机上验证产品体验。
21. 作为开发者，我想替换候选生成或排序模块而不改动 Feed 页面，以便逐步升级算法。

## Functional Requirements

### 1. 内容特征

每个片段至少包含以下字段：

- `passageId`：稳定且唯一的片段 ID。
- `bookId`、`chapterId`：所属书籍和章节。
- `topics`：一个或多个主题标签，与用户兴趣使用同一套词表。
- `mood`：主要情绪基调，可多选。
- `style`：表达方式，例如故事、对话、景物描写、观点、诗性表达。
- `difficulty`：易懂、适中、较难。
- `qualityScore`：人工或审核产生的基础质量分，范围 0 到 1。
- `editorPick`：是否为人工精选。
- `publishedAt`：进入推荐池的时间。
- `sourceLength`：原文长度，用于控制阅读负担。

标签规则：

- 每个片段最多 3 个主题标签、2 个情绪标签、1 个主要表达方式。
- 标签必须来自固定词表，不能由模型自由创建。
- 标签应描述片段的主要意义，而不是简单统计关键词。
- AI 可以辅助初标，但进入推荐池前必须由人工确认。
- 标签词表版本化，修改标签定义后保留原有版本记录。

### 2. 行为事件

第一版记录以下事件：

| 事件 | 触发时机 | 信号强度 |
|---|---|---:|
| `impression` | 片段进入可视区域并达到最短曝光时间 | 仅用于曝光统计 |
| `view_start` | 用户停留达到 1 秒 | 弱正向 |
| `view_complete` | 用户读到片段底部或停留达到内容长度阈值 | 中正向 |
| `quick_skip` | 展示后短时间内上滑 | 弱负向 |
| `like` | 点击喜欢 | 中正向 |
| `save` | 点击收藏 | 强正向 |
| `open_original` | 点击进入原文 | 强正向 |
| `continue_reading` | 在原文中继续阅读超过阈值 | 强正向 |
| `not_interested` | 点击不感兴趣 | 强负向 |
| `share` | 生成或分享片段卡 | 中正向 |

事件必须包含：事件 ID、片段 ID、发生时间、会话 ID、当前推荐来源、推荐版本、设备本地匿名用户 ID。

第一版行为存储在本机。用户登录和云同步上线后，事件使用幂等事件 ID上传，重复上传不能重复计数。

### 3. 用户偏好画像

用户画像由三部分组成：

1. 显式偏好：首次选择和后续手动修改的兴趣。
2. 短期偏好：最近一段时间内的喜欢、收藏、进入原文和完成阅读。
3. 长期偏好：经过多次行为确认后累积的主题、情绪和表达方式权重。

偏好权重采用衰减机制：近期行为影响更大，长期未发生的行为逐渐减弱。一次喜欢不能立刻把某个主题提升为长期偏好；至少需要多次一致行为或收藏行为确认。

删除喜欢、取消收藏和恢复隐藏内容时，应撤销或降低对应的偏好贡献。

### 4. 候选生成

每次生成 Feed 时，从以下候选池取内容：

- 兴趣匹配池：主题与当前偏好匹配的片段。
- 相似内容池：与用户喜欢或收藏片段在主题、情绪、文风上相似的片段。
- 编辑精选池：人工标记的高质量片段。
- 热门池：在当前时间窗口内完成阅读、收藏或进入原文较多的片段。
- 新内容池：最近加入且质量已审核的片段。
- 探索池：与当前偏好距离较远但质量合格的片段。

候选过滤条件：

- 排除用户明确隐藏的片段。
- 排除本轮已看片段。
- 默认排除近期已看片段；回顾内容需要单独标记。
- 同一本书不得连续出现超过规定数量。
- 被下架或未通过审核的片段不得进入候选池。

没有历史行为时，使用显式兴趣、编辑精选、热门和探索池；有历史行为后逐步提高相似内容和行为匹配池的比例。

### 5. 排序规则

第一版排序使用可解释规则，初始权重如下，所有权重都应配置化：

```text
score =
  topic_match       * 0.30
+ behavior_match    * 0.25
+ original_read     * 0.15
+ quality           * 0.10
+ freshness         * 0.10
+ editor_pick      * 0.05
+ exploration       * 0.05
- seen_penalty
- hidden_similarity_penalty
- repetition_penalty
```

分数不是最终 Feed 顺序的唯一依据。排序后还必须进行组装：

- 连续两条不能来自同一本书。
- 连续三条不能使用完全相同的主题组合。
- 每 4 条至少保留 1 条探索或非主要兴趣内容；用户强烈偏好形成后可以逐步调整。
- 每 10 条最多出现规定数量的同一作者或同一章节片段。
- 在不影响基本相关性的前提下优先保证内容多样性。

### 6. 推荐理由

每条片段保留一个 `reason`，用于展示轻量推荐解释，例如：

- “因为你选择了孤独与陪伴”
- “和你收藏的片段有相似主题”
- “本周读者常读”
- “编辑精选”
- “给你一段新的文学发现”

推荐理由只能来自真实触发条件，不能展示不存在的用户行为。

### 7. 推荐刷新

- 用户打开首页时，如果已有未完成 Feed，保留当前队列和阅读位置。
- 用户完成明确反馈后，仅重建未浏览尾部，不改变当前片段位置。
- 用户修改兴趣后，保留当前片段，重建后续队列。
- 用户点击“不感兴趣”后，当前片段立即移出后续队列，并提供撤销入口。
- 用户下拉刷新时生成新的推荐会话，但不能重复展示上一会话的主要内容。
- 轮次结束后显示“本轮精选已看完”，提供再看一轮和调整兴趣入口。

### 8. 评估指标

第一版不以“刷得越久越好”为唯一目标，重点指标如下：

- 推荐片段完成率：看到片段后读到结尾的比例。
- 进入原文率：点击进入原文的曝光比例。
- 原文继续阅读率：进入原文后继续阅读超过阈值的比例。
- 收藏率：曝光后收藏的比例。
- 不感兴趣率：曝光后明确拒绝的比例。
- 重复率：规定窗口内重复展示的比例。
- 主题多样性：Feed 中主题和书籍的覆盖情况。
- 次日回访率：用户第二天是否再次打开书流。

“停留时长”只作为辅助指标，并与完成率和原文继续阅读率一起解释。

### 9. 实验与基线

现有“3:1 兴趣匹配混排”保留为基线版本 `baseline_3_1`。

新推荐版本先在本地提供切换开关，用于同一用户在不同会话中比较：

- 基线：3:1 混排。
- V1：标签匹配＋行为加权＋去重。
- V1-explore：V1 加探索和人工精选。

小规模测试先观察趋势，不宣称统计显著性。正式上线实验需要记录实验组、推荐版本和实验开始时间，避免同一用户在实验过程中频繁切换策略造成数据污染。

### 10. 运营配置

第一版不建设完整后台，但内容数据必须支持：

- 修改片段标签。
- 修改质量分。
- 设置或取消人工精选。
- 设置上下线状态。
- 设置推荐黑名单。
- 查看片段的基本曝光、完成、收藏和进入原文数据。

## Implementation Decisions

- 推荐核心拆分为候选生成、特征计算、打分、Feed 组装和行为记录五个模块。
- 页面层只消费推荐结果，不直接拼接推荐规则。
- 所有推荐规则和权重使用版本号标识，便于回溯和比较。
- 片段 ID、书籍 ID 和章节 ID保持稳定，禁止依赖数组位置作为推荐对象身份。
- 本地状态继续使用现有存储层；推荐行为单独保存事件队列，避免和用户喜欢、收藏状态混在一起。
- 事件队列使用本地唯一事件 ID，未来接云端时采用“上传成功后标记”的方式，支持失败重试。
- 第一版不引入向量数据库、不引入在线模型服务、不把密钥放入 App 前端。
- 云端版本应采用用户级数据权限，推荐事件只允许本人或服务端读取。
- 用户删除账号时，应同时删除或匿名化个人行为事件和偏好画像。
- 推荐计算可以先全量扫描本地片段；当片段数量明显增长后，再引入倒排索引或预计算候选池。
- 当内容量达到数百至数千条并且用户行为足够时，再评估 LightFM 等“内容特征＋隐式反馈”模型；当候选规模和用户规模更大时，再拆成召回和排序两个服务。

## Testing Decisions

测试只验证外部行为，不绑定具体函数实现：

- 新用户选择不同兴趣时，首轮 Feed 的主题分布应符合预期。
- 无兴趣用户仍能获得非空 Feed，并包含编辑精选和探索内容。
- 喜欢、收藏、进入原文和不感兴趣后，后续未浏览队列应按规则变化。
- 快速划走不能单独造成过强负反馈；连续明确拒绝相似内容后才应明显降低相关主题。
- 当前片段的操作不会改变已浏览位置。
- Feed 不连续出现同一本书，不重复出现已隐藏片段。
- 修改兴趣只重建尾部，当前卡片保持不变。
- 推荐理由与实际触发条件一致。
- 本地存储失败时，用户状态和推荐状态回滚，并给出明确提示。
- 损坏的事件队列不会阻止 App 启动，单条损坏事件可以被跳过并记录。
- 推荐版本切换后，事件中能正确记录对应版本。
- 基线和新策略使用同一批候选时，指标计算口径一致。
- 离线期间产生的行为在恢复网络后可重复上传而不重复计数。

现有验收体系已经覆盖 Feed 切卡、兴趣、喜欢、隐藏、回退和无横向溢出等高层交互；本需求在此基础上新增推荐行为和指标断言，不直接测试内部打分函数的具体数值。

## Delivery Plan

### Phase 1：可解释推荐基础

- 固定标签词表和片段特征字段。
- 给现有内容补齐标签和质量分。
- 抽离候选、打分、组装模块。
- 保留 3:1 混排作为基线。
- 增加推荐理由。

### Phase 2：行为反馈

- 记录曝光、完成、快速划走、进入原文等事件。
- 计算本机短期偏好。
- 接入喜欢、收藏、不感兴趣和已读惩罚。
- 增加去重、同书限制和探索池。

### Phase 3：评估和内容运营

- 增加推荐版本和实验记录。
- 增加基础指标面板或导出文件。
- 建立人工精选和下架流程。
- 进行小规模真实用户测试。

### Phase 4：云端和混合模型

- 接入国内云端身份和数据服务。
- 同步用户行为和偏好画像。
- 评估内容相似模型与 LightFM 类混合模型。
- 当数据规模足够时，拆分候选召回和排序。

## Acceptance Criteria

- 新用户首次打开能在 1 秒内获得非空推荐队列。
- 现有 3:1 混排功能仍可作为基线使用。
- 用户明确点击收藏后，后续推荐中相似主题的权重提高，但不会完全占满 Feed。
- 用户点击不感兴趣后，当前片段不会再次出现在当前轮次，近似片段比例降低。
- 用户完成原文阅读后，系统记录该行为并能用于后续排序。
- 任意连续 5 条推荐中，同一本书最多出现 2 条，且不得连续出现。
- 推荐结果中至少保留配置比例的探索内容。
- 每条推荐都能显示一个真实、可解释的推荐理由。
- 推荐模块可以替换策略而不修改 Feed 页面交互。
- 断网时核心推荐继续可用，行为进入本地队列。
- 推荐数据损坏或单条事件异常时，App仍能正常启动和阅读。

## Further Notes

推荐系统的核心目标是帮助用户遇到值得继续读的片段，而不是单纯延长刷取时间。书流的差异点是“从书里的某一段文字把用户带进原书”，因此进入原文和继续阅读应当比单纯停留更重要。

第一版的权重是可验证的产品假设，不是最终答案。每次调整都应带有版本号、可观察指标和真实用户反馈。只有当内容量、行为量和测试样本足够时，机器学习模型才有机会优于规则系统。
