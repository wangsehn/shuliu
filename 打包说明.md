# 书流 · 打包与安装说明

> 版本：v1.0（对应 PRD V1.0 / first_app，验收记录见 `验收记录.md`）

## 1. 产物一览

| 产物 | 路径 | 用途 |
|---|---|---|
| Android APK（debug） | `dist/书流-v1.0-debug.apk` | 开发调试直装 |
| Android APK（release 签名） | `dist/书流-v1.0-release.apk` | 分发安装（作品集演示用） |
| PWA | 任意静态托管（如 `http://localhost:8931`） | 浏览器安装，Android/iPhone/桌面通用 |
| 应用图标源脚本 | `tools/make_icons.py`、`tools/make_android_icons.py` | 改图标后重新生成 |

## 2. Android APK 安装

1. 把 APK 传到手机（微信/网盘/数据线均可），点击打开；
2. 系统提示「未知来源应用」时，允许该来源安装；
3. 安装完成启动「书流」，数据全部保存在本机。

- release 包使用独立签名密钥 `android/app/shuliu-release.keystore`（密码 `shuliu2026`，alias `shuliu`，仅供演示，正式上架请换私密密钥）。
- debug 与 release 包签名不同，混装时需先卸载其一。

## 3. PWA 安装（免打包）

- **Android（Chrome/Edge）**：访问应用地址 → 菜单「添加到主屏幕/安装应用」→ 独立窗口运行，支持离线。
- **iPhone（Safari）**：访问应用地址 → 分享 → 「添加到主屏幕」。iOS 无法生成 ipa（需 macOS + Xcode + 开发者账号），PWA 是 iPhone 上的推荐安装方式。
- **桌面（Chrome/Edge）**：地址栏右侧「安装」图标 → 独立窗口。

## 4. 本地运行

```bash
cd test/first_app
python -m http.server 8931
# 浏览器打开 http://localhost:8931
# 测试页：/tests/unit.html?test=1  与  /tests/e2e.html
```

## 5. 复现打包全流程

```bash
cd test/first_app
node tools/build_www.js          # 同步 web 产物到 www/
npx cap sync android             # 同步到安卓工程
cd android
.\gradlew.bat assembleDebug      # 调试包
.\gradlew.bat assembleRelease    # 签名发布包
```

构建配置了国内镜像（Gradle：腾讯云；Maven：阿里云），首次构建需联网下载依赖。

## 6. 版本更新流程

1. 修改 `app.js` / `app.css` / `content/` 后，递增 `index.html` 里的 `?v=N` 版本号；
2. `node tools/build_www.js && npx cap sync android`；
3. 重新执行 `gradlew assembleRelease`；PWA 侧 Service Worker 会因新 URL 自动拉取新资源。

## 7. 技术说明

- 打包方案：Capacitor 6（`appId: com.shuliu.reader`），WebView 加载本地 `www/`，localStorage 数据与 Web 版互不干扰；
- Android 实体返回键：弹层关闭 → 应用内返回 → 首页时退出（`app.js` 内 Capacitor UA 守卫段）；
- PWA Service Worker 仅在 http(s) 非 Capacitor 环境注册，APK 内不会重复缓存；
- minSdk / targetSdk 跟随 Capacitor 6 默认值（variables.gradle）。
