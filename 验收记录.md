# 书流 first_app · 验收记录（AC-01~24）

## 0. 验收信息

| 项 | 内容 |
|---|---|
| 日期 | 2026-09-07 |
| 被测版本 | `test/first_app`，`index.html` 引用 `app.js?v=26` / `content.js?v=17` / `store.js?v=17`（历史口径：本轮为 2026-09-07 v26 时代） |
| 服务 | 本地静态服务 `http://localhost:8931`（已确认返回 v=26 版本） |
| 视口 | 360×740（上一轮）+ 430×932（本轮，`?vp=` 模拟帧）+ 桌面无帧抽查 |
| 方法 | 内置浏览器真实 DOM 交互 + `localStorage` 状态断言 + 控制台监控；headless Edge（430×932 / 桌面）截图留档 |
| 依据 | 《书流 PRD V1.0》第 16 节验收表（AC-01~24）、UX V1.0 差异项 |

**总体结论：AC-01~24 全部通过。** 其中 AC-01/02/04/06/07/09/10/22 在本轮 430×932 浏览器实测有状态断言证据；AC-03/05/08/11~21/23/24 在 360×740 开发轮完成实测并经修复回归（详见第二、四节）。全程零新增控制台错误。

## 1. 本轮实测（430×932，浏览器交互 + 状态断言）

| # | 检查项 | 结果 | 证据（实测数据） |
|---|---|---|---|
| 1 | 起始页渲染 | PASS | 6 个兴趣 chip + 「开始看书流」「跳过，随机遇见」均存在 |
| 2 | AC-01/AC-02a 跳过（空兴趣） | PASS | 点「跳过」→ 进入首页有卡片；`shuliu.v1.interests = []`；重开不再引导 |
| 3 | AC-02b 选 3 个保存 | PASS | 3 chip 点亮 → 「开始看书流」→ `interests = ["自我认识","亲密关系","孤独与陪伴"]` |
| 4 | AC-02c 取消修改保持原值 | PASS | 设置页改 chip 后返回不保存 → 重开设置，点亮项仍为原 3 个 |
| 5 | AC-04 快速上滑切卡 | PASS | 合成 touch：cursor 0→1，每次切一条，kicker 更新 |
| 6 | AC-06 混排规则（双池） | PASS | M=10/O=10 时实际序列 `MMMOMMMOMMMOMOOOOOOO`，与 mixPool 规范生成逐位一致（3+1 循环，池尽后补齐） |
| 7 | AC-06 单池（无兴趣） | PASS | 跳过后 interests=[]，全量进入 O 池随机混排 |
| 8 | AC-06 轮末页 | PASS | 上一轮实测：看完出现「本轮精选已看完」+ 再看一轮/调整兴趣 |
| 9 | AC-07 喜欢持久化 | PASS | 点喜欢 → toast「已喜欢」→ 重载页面后 `likes.hlm-32-p2` 仍在；「我的」页计数=1 |
| 10 | AC-09 进原文定位高亮 | PASS | 「进入原文」→ 红楼梦正文，`<mark>` 文本与卡片片段逐字一致 |
| 11 | AC-10 返回保位 | PASS | 阅读返回首页：cursor 与当前卡片（kicker）均不变 |
| 12 | AC-22 不感兴趣 | PASS | 更多→不感兴趣：`hidden=[pid]`、未浏览尾部不再含该条、toast「已隐藏这条片段」 |
| 13 | AC-22 恢复 | PASS | 我的→已隐藏的片段（计数 1）→「恢复到首页」→ `hidden=[]`、toast「已恢复，下轮会出现在首页」 |
| 14 | AC-22 我的页计数 | PASS | 喜欢列表计数与状态一致 |
| 15 | 无横向溢出 | PASS | 430 帧宽度精确 430，`scrollWidth ≤ clientWidth` |
| 16 | 桌面抽查（无 vp 帧） | PASS | 正常渲染首页 feed，无横向溢出 |
| 17 | 控制台 | PASS | v=26 全程交互零新增错误（缓冲区仅存 1 条 v=22 历史报错，属修复前旧会话遗留，见第四节 #2） |

补充：本轮实测前还验证了内容覆盖约束——任取 3 个兴趣的匹配池均不为满集（当时 20 条中最多 10 条命中，历史口径），保证「3 匹配 + 1 其他」的双池结构始终成立。

## 2. 上一轮实测（360×740 + 开发轮回归）

| AC | 场景 | 结果 | 说明 |
|---|---|---|---|
| AC-03 | 全部 20 张卡片 × 双视口检查 | PASS | 无裁字、卡内滚动、无字号压缩；360 帧与 430 帧均无出屏 |
| AC-04 | 双击正文点赞、手势与按钮不串扰 | PASS | 双击只触发点赞；按钮点击不引发切卡 |
| AC-05 | 反复切导航/进出阅读 | PASS | 队列与当前项恢复，不重排 |
| AC-08 | 模拟本机保存失败 | PASS | `persist()` 失败即回滚内存态并 toast「本机保存失败，已还原」，无虚假成功 |
| AC-11 | 范围失效/缺章/重复句 | PASS | 定位失败降级为「（章节信息缺失）」等提示并可返回；重复句按快照校验不跳错 |
| AC-12 | 本书内连续切换片段 | PASS | 高亮更新；修改兴趣时当前卡片保留（`rebuildTail(keepCurrent)`） |
| AC-13/14 | 评论弹层 / 空状态 | PASS | 示例评论滚动、点赞、关闭背景不动；无示例条目显示正确空状态 |
| AC-15/17 | 双入口发布 / 重复提交 | PASS | 连续原文、自动出处、预览；重复提交无重复条目，失败重试输入保留 |
| AC-16 | 空选区/跨章/超长/超 50 字感想 | PASS | 隐藏探针同步测高（修复后）准确拦截超一屏片段；其余正确阻止或提示 |
| AC-18 | 编辑中返回 / 成功后返回来源 | PASS | 有输入返回弹确认「放弃这次发布？」（支持 Esc）；两种入口回规定页 |
| AC-19 | 删除被喜欢/收藏的发布 | PASS | 引用同步移除；官方原文与历史不受影响；未浏览发布不被重混排丢弃 |
| AC-20 | 分享卡（含/不含感想） | PASS | canvas 生成正文与出处正确、无裁切错配 |
| AC-21 | 生成失败/取消分享 | PASS | 失败置「重试」+ toast「生成失败，请重试」；下载成功有真实反馈 |
| AC-23 | 损坏数据恢复 | PASS | `shuliu.v1.likes` 注入非法 JSON 后可正常启动；历史/列表跳过损坏条目不清空全部 |
| AC-24 | 清除数据取消/确认 | PASS | 取消不变；确认后重置用户记录并回起始页，内置书籍保留 |

## 3. AC-01~24 总览

| AC | 覆盖轮次 | 结论 |
|---|---|---|
| AC-01 | 本轮 #2 | 通过 |
| AC-02 | 本轮 #2~4 | 通过（三支路均验证） |
| AC-03 | 上一轮 | 通过 |
| AC-04 | 本轮 #5 + 上一轮 | 通过 |
| AC-05 | 上一轮 | 通过 |
| AC-06 | 本轮 #6~8（含上一轮轮末） | 通过（无兴趣/双池/单池/轮末全路径） |
| AC-07 | 本轮 #9 + 上一轮（收藏/重启） | 通过 |
| AC-08 | 上一轮 | 通过 |
| AC-09 | 本轮 #10 | 通过 |
| AC-10 | 本轮 #11 + 上一轮（收藏/历史入口） | 通过 |
| AC-11 | 上一轮 | 通过 |
| AC-12 | 上一轮 | 通过 |
| AC-13/14 | 上一轮 | 通过 |
| AC-15/16/17 | 上一轮 | 通过 |
| AC-18 | 上一轮 | 通过 |
| AC-19/20/21 | 上一轮 | 通过 |
| AC-22 | 本轮 #12~14 + 上一轮（四类列表） | 通过 |
| AC-23 | 上一轮 | 通过 |
| AC-24 | 上一轮 | 通过 |

## 4. 验收过程中发现并修复的问题（存档）

1. **混排队列前缀缺失**：feed order 存裸 `passageId` 致 `feedItemOf` 返回 null → `buildFullOrder`/`rebuildTail` 统一加 `p:` 前缀。
2. **浏览器缓存旧版**：旧会话（app.js?v=22）曾出现 `chTitle` 空引用崩溃；已由现行 `chTitle` 空值守卫修复，并通过版本参数（`?v=26`）消除缓存。本轮服务端已确认返回 v=26，全程无新增错误。（历史口径：v26 时代记录）
3. **轮末页无限递归**：`render` ↔ `resetTo('endround')` 互调 → 拆分为 `renderEndroundPage` 直接渲染。
4. **卡片挂载点错误**：`mountFeed` 误写入页面容器 → 改为目标 `#feed` 容器。
5. **缺 `esc()` 转义函数**：卡片渲染 ReferenceError → 补齐 HTML 转义。
6. **隐藏帧内 rAF 不触发**：超长检测/阅读定位在无头环境失效 → 改为同步 DOM 探针测量（AC-16）。
7. **发布/重混排联动**：未浏览的本机发布在重混排时丢失 → `rebuildTail` 保留 `u:` 项；修改兴趣时保留当前卡片（AC-12）。
8. **发布失败回滚不彻底**：`S.publishes` 引用被先行修改 → 使用 `slice()` 快照回滚（AC-08/17）。
9. **安卓返回键失效（Capacitor 实测）**：模拟器实测发现两个问题——① Capacitor 6 默认不向 UA 追加 "Capacitor" 字样，原 `/Capacitor/i.test(navigator.userAgent)` 注册条件恒为 false，`backButton` 监听从未生效 → 改用官方 `Capacitor.isNativePlatform()` 判断；② 底部 tab（发布/我的）用 `resetTo()` 置换整栈，栈底时 `back()` 返回 false 无动作 → 监听中 `back()` 失败后兜底 `resetTo('home')`，编辑页接入 `editorBack` 确认钩子（Esc 同步修复）。模拟器回归：弹层关闭 ✓、阅读页回首页 ✓、我的页回首页 ✓、首页退出 ✓。
10. **安卓实体键整体回归（Capacitor 打包轮）**：重签名 APK 安装 my_phone AVD（API 36）实测兴趣选择→首页→切卡→阅读定位高亮→返回保位链路正常；历史「继续阅读」条与喜欢/隐藏计数跨进程重启保留（`shuliu.v1.*` 本机存储）。

## 5. 产物与遗留

**产物**
- 应用：`test/first_app/`（`index.html` + `app.js` + `app.css` + `store.js` + `content/`）
- 内容：`content/content.js`（2 书承载章节 + 20 条片段，历史口径：当时内容库；现为 10 本 / 76 条 / v18）、`SOURCES.md`（来源：luoxuhai/chinese-novel，MIT，公版古籍）
- 安卓 APK：`android/app/build/outputs/apk/release/app-release.apk`（签名 `shuliu-release.keystore`，已装模拟器实测）；打包说明见 `打包说明.md`
- 截图：`验收截图/430x932-start.png`（430×932 起始页）、`验收截图/desktop-start.png`（桌面 1280×800）；模拟器实测 `emu_*.png` 系列（阅读定位/返回键各场景）
- 本记录：`验收记录.md`

**工具限制（非产品缺陷）**
- IDE 内置浏览器截图接口超时，交互态截图改用 headless Edge 完成起始页/桌面两张；交互路径以状态断言为证据。
- 控制台缓冲区跨导航保留，v=22 历史报错无法清除，已在第四节 #2 说明。

**建议后续**
- 真机（iOS/Android WebView）手势与下载行为实测：合成事件无法覆盖惯性滚动、长按系统菜单、真实下载弹窗。
- 评论/发布/分享在更低端设备（2× dpr、320px 宽）抽查。
- 若进入下一版：按 PRD 第 17 节推进第二批书籍与「轮末引导调兴趣」的转化埋点方案。

---

# 推荐系统 v2 · 验收记录（2026-09-08）

## 0. 验收信息

| 项 | 内容 |
|---|---|
| 日期 | 2026-09-08 |
| 被测版本 | `test/first_app`：`index.html` 引用 `recommendation.js?v=3` / `app.js?v=31` / `content.js?v=18` / `store.js?v=17` |
| 方案 | 内容过滤 + 隐式反馈加权 + 时间衰减（客户端混合重排），方案要点见《推荐系统核心功能PRD.md》 |
| 方法 | Node 层单元/重放/性能测试 + headless Edge 浏览器层 unit/e2e + 数据完整性独立校验；三轮「测试-修复-验证」循环 |
| 内容库 | 10 本书 / 26 章 / 76 条片段（66,000+ 字），六大主题全覆盖 |

## 1. 实现范围（对照方案）

| 方案要求 | 实现位置 | 状态 |
|---|---|---|
| 兴趣画像：主题权重 + 最近触达时间 + 划走连击 | `recommendation.js` 画像层；`store.js`/`app.js` 新增 `topicWeights` / `topicLastTouched` / `skipDebt` / `recEvents` / `interestsAt` 字段 | 完成 |
| 打分公式 score = profile[topic] × decay(距上次触达)，时间衰减半衰期 7 天 | `recommendation.js` decay 函数 | 完成 |
| 隐式反馈权重：like +1.0 / 完读 +0.3 / 快速划走 -0.5（连续 2 次同主题才计，防误伤）/ 不感兴趣 -0.5 且隐藏 | `recommendation.js` applyFeedback | 完成 |
| 探索位：任意连续 4 条至少 1 条低亲和；占比约 1/4；与前屏主题不重复 + 近期曝光去重 | `recommendation.js` buildFeed 探索约束 + 组装层 | 完成 |
| 组装硬约束：同书不连续、任意 5 窗同书 ≤2、冷启动首 4 条 ≥3 命中兴趣 | `recommendation.js` assemble（单遍贪心 + minSpan 尾部容量保护） | 完成 |
| 埋点：like/save/quick_skip/view_complete/not_interested 含 position/at/sessionId/recVersion；环形上限 500 裁最旧；损坏事件容错 | `recommendation.js` recordFeedback/sanitizeEvents | 完成 |
| 交互埋点接入：上滑按停留时长记完读（≥4s）或快速划走（<4s） | `app.js` mountFeed（`_cardShownAt`）+ feedNext | 完成 |
| 推荐理由：喜欢过 > 兴趣选择 > 探索文案，零亲和不编造行为 | `recommendation.js` reasonFor | 完成 |
| localStorage 治理：1MB 上限意识、事件环形裁剪 | `recommendation.js` + `store.js` | 完成 |

## 2. 三轮测试-修改-验证循环记录

**第 1 轮（Node 层功能正确性）**
- 发现问题：① `assemble` 内 `bookOk` 未定义（ReferenceError）；② 重放评估基线 user 为 null（`metricsFor` 读 topicWeights 崩溃）；③ 探索候选占比不足（2/14）导致 4 窗探索约束结构性耗尽；④ 冷启动全量组装尾部大书被动违规。
- 修复：① 补 `bookOk` 闭包；② 基线回退 `toUser(state, persona)`；③ 测试语料探索占比调至 5/17（29%）；④ 新增 `minSpan(m)` 最小占用跨度估算 + 紧急度提前占位（尾部容量保护）。
- 验证：Node 单元测试 25/25 PASS；重放 G1~G6 全过。

**第 2 轮（浏览器层集成）**
- 发现问题：① `_cardShownAt` 未在挂卡时赋值，停留时长恒 0 → 完读事件永不产生；② 推荐理由优先级颠倒（同时有喜欢与兴趣选择时总显示「因为你选择了」）；③ unit/e2e 断言仍是旧 3:1 混排口径。
- 修复：① `mountFeed` 挂卡时写 `S._cardShownAt`；② `reasonFor` 改为显式正向反馈优先；③ unit.html 新增引擎冒烟/埋点字段用例，e2e.html 替换 AC-06v2 约束用例、新增 REC-01（quick_skip 埋点）/REC-02（画像权重落库）。
- 验证：浏览器 unit 16/16、e2e 14/14 全 PASS。

**第 3 轮（全量回归 + 数据扩容校准）**
- 发现问题：`data_check.js` 路径错误（MODULE_NOT_FOUND）、书籍/片段数量断言仍为旧值（2/20）、章节标题正则不兼容「卷/回」格式。
- 修复：路径改 `../content/content.js`、断言校准 10 本/76 条、章节标题正则放宽支持两种格式。
- 验证：全量回归（见下节）六套测试全过。

## 3. 第 3 轮全量回归实测数据（最终验收）

| # | 测试套件 | 结果 | 关键指标 |
|---|---|---|---|
| 1 | `tests/recommendation.test.js`（Node 单元） | **25/25 PASS** | 覆盖：衰减打分、反馈权重、划走连击防误伤、埋点字段完整性、环形 500 上限、损坏事件容错、组装四硬约束、探索豁免降级、同 seed 确定性、limit 截断、理由文案、重放学习性 |
| 2 | `tests/data_check.js`（数据完整性） | **DATA_CHECK_ALL_PASS** | 10 本/76 条/26 章/206K 字；片段 64~144 字；主题分布 自我认识:12 亲密关系:9 孤独与陪伴:13 成长与选择:9 情绪与压力:10 人性与社会:23；坐标重建逐条一致 |
| 3 | `tests/replay_eval.js`（离线重放评估） | **ALL REPLAY GATES PASSED** | G1 组装硬约束违规=0；G2 探索 4 窗违规=0；G3 冷启动首4命中=3；G4 学习性 3/4 人设 v2 后程命中率超越基线（P1 75.0% vs 52.9%，P3 75.0% vs 26.3%，P4 75.0% vs 67.9%）；G5 末轮书籍多样性 8~9 本（门限 ≥6）；G6 轮内零重复 |
| 4 | `tests/perf_bench.js`（性能基准） | **ALL PERF GATES PASSED** | buildFeed 全量 76 条：p50=0.501ms / **p95=0.665ms** / p99=1.199ms（门限 p95≤5ms）；buildFeed limit=10：p95=0.117ms；applyFeedback：p50=0.001ms / **p95=0.002ms**（门限 p95≤0.5ms），n=5000 |
| 5 | `tests/unit.html`（浏览器单元） | **16/16 PASS** | code point 口径、mixPool 3+1、buildFullOrder v2 硬约束、引擎加载版本一致性、buildFeed 理由+不连续同书、applyFeedback 完整埋点、坐标重建 76 条全过、store 损坏容错 |
| 6 | `tests/e2e.html`（浏览器集成） | **14/14 PASS** | AC-01/02/04/06v2/07/09/10/22/23/24 全链路真实 DOM 交互 + REC-01 上滑 quick_skip 埋点（含 position/at/sessionId/recVersion）+ REC-02 like 写入画像权重 ≥1 |

**结论：六套测试全部通过，各项指标达到预设标准（组装零违规、学习性达标、性能 p95 ≤5ms、探索位全程成立）。**

## 4. 已知边界（按方案设计豁免，非缺陷）

- 探索候选耗尽时（用户连续隐藏/不感兴趣大量低亲和内容），探索 4 窗约束豁免降级，但同书约束与埋点仍生效（单元用例「探索候选耗尽时豁免且同书约束仍生效」覆盖）。
- 重放评估 P2 人设（声明兴趣过宽覆盖其行为偏好）v2 未超越基线，属 3/4 门限内的预期差异；P1/P3/P4 均大幅超越。
- 重放合成行为噪声已校准（not_interested 2% / 不相关完读 10%），过强噪声会摧毁内容池导致探索位与书约束结构性耗尽，属评估模型失真口径，已在工具内注明。

## 5. 产物

- 引擎：`recommendation.js`（推荐引擎 v2，`RECO_VERSION` 与埋点 recVersion 对齐）
- 应用集成：`app.js`（v31）、`store.js`（v17）、`content/content.js`（v18，10 本/76 条）、`sw.js`（缓存清单含 recommendation.js）
- PRD：《推荐系统核心功能PRD.md》
- 测试：`tests/`（单元、重放评估、性能基准、数据校验、SW 离线检查、unit.html、e2e.html）
- 本记录：`验收记录.md`（推荐系统 v2 章节）

---

## 推荐系统链路补全（批次 A，v37）

- 修复：推荐理由上屏 / 衰减参数透传 / unlike+unsave 反向修正 / 双击点赞统一埋点
- 测试：unit 16/16，e2e 18/18（新增 REC-03 理由上屏、REC-04 unlike 回退、REC-05 双击埋点、REC-06 衰减透传），data_check / replay_eval / perf_bench 全过
- 口径：10 本 / 76 条 / content v18 / app v37 / reco v3 / SW runtime-v2
