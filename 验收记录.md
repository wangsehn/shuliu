# 书流 first_app · 验收记录（AC-01~24）

## 0. 验收信息

| 项 | 内容 |
|---|---|
| 日期 | 2026-09-07 |
| 被测版本 | `test/first_app`，`index.html` 引用 `app.js?v=26` / `content.js?v=17` / `store.js?v=17` |
| 服务 | 本地静态服务 `http://localhost:8931`（已确认返回 v=26 版本） |
| 视口 | 360×740（上一轮）+ 430×932（本轮，`?vp=` 模拟帧）+ 桌面无帧抽查 |
| 方法 | 内置浏览器真实 DOM 交互 + `localStorage` 状态断言 + 控制台监控；headless Edge（430×932 / 桌面）截图留档 |
| 依据 | 《书流 PRD V1.0》第 16 节验收表（AC-01~24）、UX V1.0 差异项 |

**总体结论：AC-01~24 全部通过。** 其中 AC-01/02/04/06/07/09/10/22 在本轮 430×932 浏览器实测有状态断言证据；AC-03/05/08/11~21/23/24 在 360×740 开发轮完成实测并经修复回归（详见第二、四节）。全程零新增控制台错误。

## 1. 本轮实测（430×932，浏览器交互 + 状态断言）

| # | 检查项 | 结果 | 证据（实测数据） |
|---|---|---|---|
| 1 | 起始页渲染 | PASS | 6 个兴趣 chip + 「开始看书流」「跳过，随机遇见」均存在 |
| 2 | AC-01/AC-02a 跳过（空兴趣） | PASS | 点「跳过」→ 进入首页有卡片；`shuliu.v1.interests = []`；重开不再引导 |
| 3 | AC-02b 选 3 个保存 | PASS | 3 chip 点亮 → 「开始看书流」→ `interests = ["自我认识","亲密关系","孤独与陪伴"]` |
| 4 | AC-02c 取消修改保持原值 | PASS | 设置页改 chip 后返回不保存 → 重开设置，点亮项仍为原 3 个 |
| 5 | AC-04 快速上滑切卡 | PASS | 合成 touch：cursor 0→1，每次切一条，kicker 更新 |
| 6 | AC-06 混排规则（双池） | PASS | M=10/O=10 时实际序列 `MMMOMMMOMMMOMOOOOOOO`，与 mixPool 规范生成逐位一致（3+1 循环，池尽后补齐） |
| 7 | AC-06 单池（无兴趣） | PASS | 跳过后 interests=[]，全量进入 O 池随机混排 |
| 8 | AC-06 轮末页 | PASS | 上一轮实测：看完出现「本轮精选已看完」+ 再看一轮/调整兴趣 |
| 9 | AC-07 喜欢持久化 | PASS | 点喜欢 → toast「已喜欢」→ 重载页面后 `likes.hlm-32-p2` 仍在；「我的」页计数=1 |
| 10 | AC-09 进原文定位高亮 | PASS | 「进入原文」→ 红楼梦正文，`<mark>` 文本与卡片片段逐字一致 |
| 11 | AC-10 返回保位 | PASS | 阅读返回首页：cursor 与当前卡片（kicker）均不变 |
| 12 | AC-22 不感兴趣 | PASS | 更多→不感兴趣：`hidden=[pid]`、未浏览尾部不再含该条、toast「已隐藏这条片段」 |
| 13 | AC-22 恢复 | PASS | 我的→已隐藏的片段（计数 1）→「恢复到首页」→ `hidden=[]`、toast「已恢复，下轮会出现在首页」 |
| 14 | AC-22 我的页计数 | PASS | 喜欢列表计数与状态一致 |
| 15 | 无横向溢出 | PASS | 430 帧宽度精确 430，`scrollWidth ≤ clientWidth` |
| 16 | 桌面抽查（无 vp 帧） | PASS | 正常渲染首页 feed，无横向溢出 |
| 17 | 控制台 | PASS | v=26 全程交互零新增错误（缓冲区仅存 1 条 v=22 历史报错，属修复前旧会话遗留，见第四节 #2） |

补充：本轮实测前还验证了内容覆盖约束——任取 3 个兴趣的匹配池均不为满集（20 条中最多 10 条命中），保证「3 匹配 + 1 其他」的双池结构始终成立。

## 2. 上一轮实测（360×740 + 开发轮回归）

| AC | 场景 | 结果 | 说明 |
|---|---|---|---|
| AC-03 | 全部 20 张卡片 × 双视口检查 | PASS | 无裁字、卡内滚动、无字号压缩；360 帧与 430 帧均无出屏 |
| AC-04 | 双击正文点赞、手势与按钮不串扰 | PASS | 双击只触发点赞；按钮点击不引发切卡 |
| AC-05 | 反复切导航/进出阅读 | PASS | 队列与当前项恢复，不重排 |
| AC-08 | 模拟本机保存失败 | PASS | `persist()` 失败即回滚内存态并 toast「本机保存失败，已还原」，无虚假成功 |
| AC-11 | 范围失效/缺章/重复句 | PASS | 定位失败降级为「（章节信息缺失）」等提示并可返回；重复句按快照校验不跳错 |
| AC-12 | 本书内连续切换片段 | PASS | 高亮更新；修改兴趣时当前卡片保留（`rebuildTail(keepCurrent)`） |
| AC-13/14 | 评论弹层 / 空状态 | PASS | 示例评论滚动、点赞、关闭背景不动；无示例条目显示正确空状态 |
| AC-15/17 | 双入口发布 / 重复提交 | PASS | 连续原文、自动出处、预览；重复提交无重复条目，失败重试输入保留 |
| AC-16 | 空选区/跨章/超长/超 50 字感想 | PASS | 隐藏探针同步测高（修复后）准确拦截超一屏片段；其余正确阻止或提示 |
| AC-18 | 编辑中返回 / 成功后返回来源 | PASS | 有输入返回弹确认「放弃这次发布？」（支持 Esc）；两种入口回规定页 |
| AC-19 | 删除被喜欢/收藏的发布 | PASS | 引用同步移除；官方原文与历史不受影响；未浏览发布不被重混排丢弃 |
| AC-20 | 分享卡（含/不含感想） | PASS | canvas 生成正文与出处正确、无裁切错配 |
| AC-21 | 生成失败/取消分享 | PASS | 失败置「重试」+ toast「生成失败，请重试」；下载成功有真实反馈 |
| AC-23 | 损坏数据恢复 | PASS | `shuliu.v1.likes` 注入非法 JSON 后可正常启动；历史/列表跳过损坏条目不清空全部 |
| AC-24 | 清除数据取消/确认 | PASS | 取消不变；确认后重置用户记录并回起始页，内置书籍保留 |

## 3. AC-01~24 总览

| AC | 覆盖轮次 | 结论 |
|---|---|---|
| AC-01 | 本轮 #2 | 通过 |
| AC-02 | 本轮 #2~4 | 通过（三支路均验证） |
| AC-03 | 上一轮 | 通过 |
| AC-04 | 本轮 #5 + 上一轮 | 通过 |
| AC-05 | 上一轮 | 通过 |
| AC-06 | 本轮 #6~8（含上一轮轮末） | 通过（无兴趣/双池/单池/轮末全路径） |
| AC-07 | 本轮 #9 + 上一轮（收藏/重启） | 通过 |
| AC-08 | 上一轮 | 通过 |
| AC-09 | 本轮 #10 | 通过 |
| AC-10 | 本轮 #11 + 上一轮（收藏/历史入口） | 通过 |
| AC-11 | 上一轮 | 通过 |
| AC-12 | 上一轮 | 通过 |
| AC-13/14 | 上一轮 | 通过 |
| AC-15/16/17 | 上一轮 | 通过 |
| AC-18 | 上一轮 | 通过 |
| AC-19/20/21 | 上一轮 | 通过 |
| AC-22 | 本轮 #12~14 + 上一轮（四类列表） | 通过 |
| AC-23 | 上一轮 | 通过 |
| AC-24 | 上一轮 | 通过 |

## 4. 验收过程中发现并修复的问题（存档）

1. **混排队列前缀缺失**：feed order 存裸 `passageId` 致 `feedItemOf` 返回 null → `buildFullOrder`/`rebuildTail` 统一加 `p:` 前缀。
2. **浏览器缓存旧版**：旧会话（app.js?v=22）曾出现 `chTitle` 空引用崩溃；已由现行 `chTitle` 空值守卫修复，并通过版本参数（`?v=26`）消除缓存。本轮服务端已确认返回 v=26，全程无新增错误。
3. **轮末页无限递归**：`render` ↔ `resetTo('endround')` 互调 → 拆分为 `renderEndroundPage` 直接渲染。
4. **卡片挂载点错误**：`mountFeed` 误写入页面容器 → 改为目标 `#feed` 容器。
5. **缺 `esc()` 转义函数**：卡片渲染 ReferenceError → 补齐 HTML 转义。
6. **隐藏帧内 rAF 不触发**：超长检测/阅读定位在无头环境失效 → 改为同步 DOM 探针测量（AC-16）。
7. **发布/重混排联动**：未浏览的本机发布在重混排时丢失 → `rebuildTail` 保留 `u:` 项；修改兴趣时保留当前卡片（AC-12）。
8. **发布失败回滚不彻底**：`S.publishes` 引用被先行修改 → 使用 `slice()` 快照回滚（AC-08/17）。
9. **安卓返回键失效（Capacitor 实测）**：模拟器实测发现两个问题——① Capacitor 6 默认不向 UA 追加 "Capacitor" 字样，原 `/Capacitor/i.test(navigator.userAgent)` 注册条件恒为 false，`backButton` 监听从未生效 → 改用官方 `Capacitor.isNativePlatform()` 判断；② 底部 tab（发布/我的）用 `resetTo()` 置换整栈，栈底时 `back()` 返回 false 无动作 → 监听中 `back()` 失败后兜底 `resetTo('home')`，编辑页接入 `editorBack` 确认钩子（Esc 同步修复）。模拟器回归：弹层关闭 ✓、阅读页回首页 ✓、我的页回首页 ✓、首页退出 ✓。
10. **安卓实体键整体回归（Capacitor 打包轮）**：重签名 APK 安装 my_phone AVD（API 36）实测兴趣选择→首页→切卡→阅读定位高亮→返回保位链路正常；历史「继续阅读」条与喜欢/隐藏计数跨进程重启保留（`shuliu.v1.*` 本机存储）。

## 5. 产物与遗留

**产物**
- 应用：`test/first_app/`（`index.html` + `app.js` + `app.css` + `store.js` + `content/`）
- 内容：`content/content.js`（2 书承载章节 + 20 条片段）、`SOURCES.md`（来源：luoxuhai/chinese-novel，MIT，公版古籍）
- 安卓 APK：`android/app/build/outputs/apk/release/app-release.apk`（签名 `shuliu-release.keystore`，已装模拟器实测）；打包说明见 `打包说明.md`
- 截图：`验收截图/430x932-start.png`（430×932 起始页）、`验收截图/desktop-start.png`（桌面 1280×800）；模拟器实测 `emu_*.png` 系列（阅读定位/返回键各场景）
- 本记录：`验收记录.md`

**工具限制（非产品缺陷）**
- IDE 内置浏览器截图接口超时，交互态截图改用 headless Edge 完成起始页/桌面两张；交互路径以状态断言为证据。
- 控制台缓冲区跨导航保留，v=22 历史报错无法清除，已在第四节 #2 说明。

**建议后续**
- 真机（iOS/Android WebView）手势与下载行为实测：合成事件无法覆盖惯性滚动、长按系统菜单、真实下载弹窗。
- 评论/发布/分享在更低端设备（2× dpr、320px 宽）抽查。
- 若进入下一版：按 PRD 第 17 节推进第二批书籍与「轮末引导调兴趣」的转化埋点方案。
